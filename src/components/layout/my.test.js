import React from 'react';
import { Card } from './Card';
import { shallow } from 'enzyme';

describe('some', () => {
  let wrapper;
  it('something', () => {
    wrapper = shallow(<Card />);
  });
});
